-------------------------------------------------------------------------------
Mixed-integer second-order cone preprocessing experiment
-------------------------------------------------------------------------------

This tool will have to be compiled from source using the GNU Makefile.

  make clean all -f Makefile.pptool


Usage examples:

  Presolve files in CBF format and write result to subdir 'presolved':
    pptool -t presolve -opath presolved CBFFILE1 CBFFILE2 CBFFILE3 ...


